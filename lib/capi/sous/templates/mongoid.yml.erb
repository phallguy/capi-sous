production:
  sessions:
    default:
      database: <%= mongoid_db_name %>
      hosts:
        - <%= mongoid_server %>
      options:
        safe: true

  options:
    preload_models: true
    use_utc: true
